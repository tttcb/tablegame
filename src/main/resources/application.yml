server:
  port: 8080
spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://60.204.207.251:3306/guiguohui?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
      username: root
      password: 163.com
  application:
    name: GuiGuoHui-system
  profiles:
    active: dev
  servlet:
    multipart:
      enabled: true
      max-file-size: 30MB
      max-request-size: 30MB

jwt:
  tokenHeader: Authorization #JWT存储的请求头
  secret: codingmore-admin-secret #JWT加解密使用的密钥
  expiration: 10800 #JWT的超期限时间(60*60*2)
  tokenHead: 'Bearer '  #JWT负载中拿到开头
secure:
  ignored:
    urls: #安全路径白名单
      - /guiguohui/login
      - /swagger-resources/**
      - /v3/api-docs/**
      - /webjars/**
      - /doc.html/**
      - /guiguohui/commodity/loadImage/**
      - /**